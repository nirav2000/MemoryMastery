{
  "name": "memory-mastery-quality-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "test:entry": "python3 tests/entry-points.spec.py",
    "test:journeys": "python3 tests/critical-journeys.spec.py",
    "test:browser": "npm run test:entry && npm run test:journeys",
    "test:visual": "for shard in $(seq 0 29); do python3 tests/visual-matrix.spec.py --shard $shard --shard-count 30 || exit 1; done"
  },
  "devDependencies": {
    "axe-core": "4.13.0"
  }
}
